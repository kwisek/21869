apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kalimba-server.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "kalimba-server.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "kalimba-server.fullname" . }}
    spec:
      containers:
        - name: kalimba-server
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
